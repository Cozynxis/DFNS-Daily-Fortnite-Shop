/* DFNS reaction backend configuration.
 *
 * Create a free Supabase project, then put the Project URL and the anon/public key
 * below. The anon key is intended for browser use; NEVER put a service_role key here.
 */
window.DFNS_REACTIONS_CONFIG = {
  url: "",
  anonKey: ""
};
