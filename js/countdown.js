document.addEventListener('DOMContentLoaded',()=>{const modes={br:{eyebrow:'BATTLE ROYALE',name:'Chapter 7 — Season 4',start:'2026-08-20T00:00:00+02:00',end:'2026-11-01T00:00:00+01:00',next:'Chapter 7 — Season 5',theme:'br'},og:{eyebrow:'FORTNITE OG',name:'OG Season 9',start:'2026-06-25T00:00:00+02:00',end:'2026-09-02T00:00:00+02:00',next:'OG Season X',theme:'og'},festival:{eyebrow:'FESTIVAL',name:'Season 15 — Lil Tecca',start:'2026-07-29T00:00:00+02:00',end:'2026-10-31T00:00:00+01:00',next:'Festival Season 16',theme:'festival'},lego:{eyebrow:'LEGO FORTNITE',name:'Ninjago 2',start:'2026-04-16T00:00:00+02:00',end:'2026-09-02T00:00:00+02:00',next:'Next LEGO Pass',theme:'lego'}};const $=id=>document.getElementById(id);let current='br';const fmtDate=d=>new Intl.DateTimeFormat('en-US',{month:'long',day:'numeric',year:'numeric'}).format(d);const update=()=>{const m=modes[current],now=new Date(),start=new Date(m.start),end=new Date(m.end);let total=end-start,elapsed=Math.max(0,Math.min(total,now-start)),remaining=Math.max(0,end-now),pct=Math.max(0,Math.min(100,elapsed/total*100));const totalSec=Math.floor(remaining/1000),days=Math.floor(totalSec/86400),hours=Math.floor(totalSec%86400/3600),minutes=Math.floor(totalSec%3600/60),seconds=totalSec%60; $('days').textContent=String(days).padStart(2,'0');$('hours').textContent=String(hours).padStart(2,'0');$('minutes').textContent=String(minutes).padStart(2,'0');$('seconds').textContent=String(seconds).padStart(2,'0');$('mode-eyebrow').textContent=m.eyebrow;$('season-name').textContent=m.name;$('season-dates').textContent=`${fmtDate(start)} — ${fmtDate(end)}`;$('end-date').textContent=fmtDate(end);$('next-season').textContent=m.next;$('duration').textContent=`${Math.round(total/86400000)} days`;$('progress-percent').textContent=`${pct.toFixed(0)}%`;$('progress-bar').style.width=`${pct}%`;$('elapsed-text').textContent=`${Math.floor(elapsed/86400000)} days elapsed`;$('remaining-text').textContent=`${Math.ceil(remaining/86400000)} days remaining`;$('season-status-text').textContent=remaining>0?'CURRENT SEASON':'SEASON ENDED';$('countdown-updated').textContent=`Updated ${now.toLocaleTimeString([], {hour:'2-digit',minute:'2-digit',second:'2-digit'})}`;$('countdown-card').dataset.theme=m.theme};document.querySelectorAll('.countdown-tab').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.countdown-tab').forEach(b=>{b.classList.remove('active');b.setAttribute('aria-selected','false')});btn.classList.add('active');btn.setAttribute('aria-selected','true');current=btn.dataset.mode;update()}));update();setInterval(update,1000);});